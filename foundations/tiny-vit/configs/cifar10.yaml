# CIFAR-10 config for ViT
dataset_name: cifar10
batch_size: 32
image_size: 224  # ViT-B/16 expects 224x224
num_classes: 10
normalization: imagenet  # imagenet for pretrained ViT; cifar10 for training from scratch

# Model (timm)
model_name: vit_base_patch16_224
pretrained: true
