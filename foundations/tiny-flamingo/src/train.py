"""Training loop for tiny-flamingo (Perceiver Resampler only).

TODO: Implement training
- ViT + GPT-2 frozen
- Train resampler only
- Loss: next-token prediction
- AMP, gradient checkpointing if needed
"""
